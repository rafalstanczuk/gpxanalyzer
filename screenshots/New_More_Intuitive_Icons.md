# Refreshed Overlay UI — Material Symbols (Set 1)
New Material Symbols icons for database, gradient, and group work overlays with programmatic FABs — no XML per layout variant, consistent 48dp touch targets.
