# Detalle a nivel de sendero — teselas + mapa de calor
Vista ampliada de los puntos de elevación decodificados de los canales RGB de los píxeles, con caché de tres niveles (LRU en memoria, base Room, AWS S3) y búsquedas por debajo del milisegundo.
