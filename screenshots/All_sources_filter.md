# 4-State Source Filter — API / Track / Tile / All
Heatmap with all elevation sources enabled, showing the 4-state source filter that cycles through API-fetched, GPX track, terrain tile, and combined views with one tap.
