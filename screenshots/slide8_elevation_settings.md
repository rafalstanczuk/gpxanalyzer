# Elevation Settings — Full Pipeline Control
Elevation Settings dialog showing data source strategy selection (API, cached, or cache-first), source filter by origin, live cache coverage statistics, and auto-import toggle.
