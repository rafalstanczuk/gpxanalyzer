# Heatmap Overlay — API Only, Elevation Metric
Heatmap switched to elevation metric with API-only source filter, showing average elevation per grid cell with a blue-to-red gradient dynamically scaled to the visible data range.
