# Heatmap Overlay — GPX Only, Elevation Metric
Heatmap showing mean elevation per cell using only GPX-imported data, with visible data point markers and a gradient legend showing the elevation range from the GPX file source.
