# Dettaglio a livello di sentiero — tessere + heatmap
Vista ingrandita dei punti di quota decodificati dai canali RGB dei pixel, con cache a tre livelli (LRU in memoria, database Room, AWS S3) e ricerche in meno di un millisecondo.
