# Elevation Settings
Elevation Settings dialog showing data source strategy selection (API, cached, or cache-first), cache source filtering by origin, live cache coverage statistics, and auto-import toggle for GPX file elevation data.
