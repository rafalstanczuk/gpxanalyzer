New Features:
• Users can now open .gpx files directly in the app via the system's 'Open with' menu.

Bug Fixes:
• Since 2.8.0 - 2.8.6 : 
• 1. Extrema segment detector loads default segment detection instead of customized per domain and activity type
• 2. Strava API wrong logic fixes
• 3. Fix Gpx parser to be compliant with Gpx 1.1 standard
• 4. The Author determination should be using existing token
• 5. If synthetic timestamp are generated, then don't loose any points by computing mean location, or speed.
